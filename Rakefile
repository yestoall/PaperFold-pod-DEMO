# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require "motion/project"
require "teacup"
require "sugarcube"
require "sweettea"
require "motion-cocoapods"

Motion::Project::App.setup do |app|
  app.name             = "PaperFold-pod-DEMO"
  app.version          = "1.0"
  app.pods do
    pod "PaperFold-pod", :git => "https://github.com/yestoall/PaperFold-pod.git"
  end
end
